name: Build and publish

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    uses: ./.github/workflows/build-release.yml
    with:
      publish_itch: true
    secrets: inherit
